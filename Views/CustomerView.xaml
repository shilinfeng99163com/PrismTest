<UserControl x:Class="PrismTest.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PrismTest.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="客户管理" FontSize="18" FontWeight="Bold"
                   Margin="10" HorizontalAlignment="Center" />

        <!-- 客户列表 -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Customers}"
                  SelectedItem="{Binding SelectedCustomer}"
                  AutoGenerateColumns="False" Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="姓名" Binding="{Binding Name}" Width="*" />
                <DataGridTextColumn Header="邮箱" Binding="{Binding Email}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <!-- 操作按钮 -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
            <Button Content="加载客户" Command="{Binding LoadCustomersCommand}" Margin="5" Padding="10,5" />
            <Button Content="添加客户" Command="{Binding AddCustomerCommand}" Margin="5" Padding="10,5" />
            <Button Content="删除客户" Command="{Binding DeleteCustomerCommand}" Margin="5" Padding="10,5" />
        </StackPanel>
    </Grid>
</UserControl>